{
    "name": "gemini-svg-lambda",
    "version": "1.0.0",
    "description": "Standalone Lambda for SVG generation",
    "main": "dist/index.js",
    "scripts": {
        "build": "esbuild index.ts --bundle --platform=node --target=node20 --outfile=dist/index.js --external:aws-lambda"
    },
    "dependencies": {
        "@ai-sdk/google": "^0.0.55",
        "ai": "^3.4.33",
        "posthog-node": "^5.14.0"
    },
    "devDependencies": {
        "@types/aws-lambda": "^8.10.145",
        "@types/node": "^20.0.0",
        "esbuild": "^0.24.0"
    }
}

// Credit cost configuration - can be moved to env vars if you need per-environment overrides
export const CREDIT_COSTS = {
  FLASH: 1,
  PRO: 3,
  PRO_31: 5,
} as const;

export const FREE_PRO_GENERATIONS = 3;
